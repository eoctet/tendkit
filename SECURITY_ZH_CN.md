# 安全策略

[English](SECURITY.md) | [简体中文](SECURITY_ZH_CN.md)

## 支持版本

TendKit 仍处于预发布阶段。安全修复只提供给最新 tag release 和当前 `main` 分支；旧版本与不再维护的分支不接收安全更新。

| 版本 | 是否支持 |
| --- | --- |
| 最新 tag release | 是 |
| `main` | 是 |
| 旧版本或其他分支 | 否 |

项目建立稳定发布节奏后可能调整本策略。

## 报告安全漏洞

不要为尚未公开的漏洞创建公开 Issue、Discussion 或 Pull Request。

请使用 GitHub 的[私有漏洞报告](https://github.com/eoctet/tendkit/security/advisories/new)将报告发送给维护者。如果该入口不可用，可以创建一个不含漏洞细节或敏感信息的公开 Issue，请维护者建立私下联系渠道。

在安全且可获得的前提下，请提供：

- 受影响版本、commit、操作系统和架构；
- 受影响组件与安全边界；
- 复现步骤或最小概念验证；
- 预期行为与实际行为；
- 潜在影响与必要前置条件；
- 已知的缓解或修复建议；
- 问题是否已在其他位置披露；
- 可用于后续联系的安全方式。

不要提交真实凭据、令牌、加密密钥、个人数据或生产配置。请使用合成数据，并从日志、路径、环境变量和命令输出中删除敏感内容。

## 处理预期

维护者的目标是：

- 在 3 个工作日内确认收到完整报告；
- 在 10 个工作日内给出初步评估或请求补充信息；
- 对已接受但尚未解决的报告，至少每 7 天提供一次状态更新。

以上是响应目标，不是修复期限保证。解决时间取决于严重程度、可复现性、支持平台以及安全发布所需的协调工作。

完成初步评估后，维护者会告知报告是否被接受、是否需要更多信息、是否属于已知问题或是否不在本策略范围。对于接受的漏洞，维护者会协调验证、缓解、发布时间和披露。请在公开细节前给予合理的修复时间。

## 适用范围

安全报告包括但不限于：

- 通过配置、模板、Provider 数据或扫描 metadata 形成的命令或参数注入；
- 凭据、令牌、加密密钥或环境变量的不安全传递与日志记录；
- 配置所有权、权限、锁或原子持久化行为不安全；
- 下载源混淆、路径穿越、不安全制品选择或完整性校验绕过；
- 取消或进程组处理失败，导致高权限或破坏性子进程继续运行；
- 跨越明确文档化信任边界的网络或解析问题。

以下情况通常不能单独构成漏洞：

- 受信任的用户自定义 Provider action 执行了其中明确写出的命令；
- 发现了较新工具版本，但相应 Provider 没有更新能力；
- 不支持的操作系统、架构或旧 TendKit 版本；
- 社会工程、物理访问或用户账户、工作站已经被攻破；
- 只有依赖扫描告警，但不能证明其影响受支持 TendKit 版本的可达路径。

如果无法判断问题是否涉及安全，请优先私下报告。

## 安全研究

只测试你拥有或已获授权的系统与数据。避免侵犯隐私、服务中断、数据破坏、建立持久化，以及超出证明问题所需范围的访问。不要针对其他用户或第三方服务测试。遵守这些规则的善意报告会按安全研究处理，而不是滥用行为。

## 披露与致谢

项目倾向协调披露。修复或缓解措施可用后，维护者可能发布 GitHub Security Advisory 和 release notes，说明受影响版本、影响和修复方法，同时避免暴露不必要的利用细节。报告者提出请求且情况适合时会获得致谢；项目也尊重匿名报告和匿名致谢。
